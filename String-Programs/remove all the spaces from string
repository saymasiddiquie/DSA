#include <bits/stdc++.h>
using namespace std;

int main() {
    string n;
    getline(cin, n);
    string result=" ";
    for(int i=0;i<n.length();i++){
        if(n[i]!=' '){
            result+=n[i];
        }
    }
    cout<<result;
}
