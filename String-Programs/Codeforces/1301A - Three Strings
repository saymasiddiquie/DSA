#include <bits/stdc++.h>
using namespace std;

int main() {
    int t;
    cin >> t;
    
    while(t--) {
        string a, b, c;
        cin >> a >> b >> c;
        
        bool possible = true;
        
        for(int i = 0; i < a.size(); i++) {
            if(c[i] != a[i] && c[i] != b[i]) {
                possible = false;
                break;
            }
        }
        
        if(possible) cout << "YES\n";
        else cout << "NO\n";
    }
    
    return 0;
}
