#include <bits/stdc++.h>
using namespace std;

int main() {
    int n;
    cin>>n;
	int arr[n];
	for(int i=0;i<n;i++){
	    cin>>arr[i];
	}
	bool found =false;
	for(int i=0;i<n;i++){
	    if(arr[i]== 6){
	        found=true;
	        break;
	    }
	    
	}
	if(found){
	    cout<<"found";
	}
	else{
	    cout<<"not found";
	}
}
