##This is right rotation
#include <bits/stdc++.h>
using namespace std;

void rotate(int arr[], int n, int k) {

    k = k % n;   // handle large k

    // step 1
    reverse(arr, arr + n - k);

    // step 2
    reverse(arr + n - k, arr + n);

    // step 3
    reverse(arr, arr + n);
}

int main() {
    int n;
    cin >> n;

    int arr[n];

    for(int i = 0; i < n; i++) {
        cin >> arr[i];
    }

    int k;
    cin >> k;

    rotate(arr, n, k);

    for(int i = 0; i < n; i++) {
        cout << arr[i] << " ";
    }

    return 0;
}
