#include <bits/stdc++.h>
using namespace std;

int main() {
    int n;
    cin>>n;
    int arr[n];
    for(int i=0;i<n;i++){
        cin>>arr[i];
    }
    int left=0;
    int right=n-1;
    bool palindrome=true;
    while(left<right){
        if(arr[left]!=arr[right]){
            palindrome=false;
            break;
            
        }
        left++;
        right--;
    }
    if(palindrome==false){
        cout<<" is not a palindrome";
    }
    else{
        cout<<"is a palindrome";
    }

}
