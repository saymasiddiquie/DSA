#include <bits/stdc++.h>

using namespace std;

int main() {
    string s;
    cin >> s;
    string reverse="";
    for(int i=0;i<s.length();i++){
        if(s[i]=='0'){
            continue;
        }
        for(int j=i+1;j<s.length();j++){
            if(s[i]==s[j]){
                cout<<s[i];
                s[j]='0';
                
            }
        }
    }

}
